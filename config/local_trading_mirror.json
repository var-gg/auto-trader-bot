{
  "schema": "trading",
  "mirror_tables": [
    {
      "name": "ticker",
      "source_table": "trading.ticker",
      "target_table": "trading.bt_mirror_ticker",
      "refresh_group": "reference",
      "refresh_strategy": "full",
      "source_sql": "SELECT id, symbol, exchange, country FROM trading.ticker",
      "insert_sql": "INSERT INTO trading.bt_mirror_ticker(ticker_id, symbol, exchange, country) VALUES (:id, :symbol, :exchange, :country)",
      "truncate_sql": "TRUNCATE TABLE trading.bt_mirror_ticker"
    },
    {
      "name": "ohlcv_daily",
      "source_table": "trading.ohlcv_daily",
      "target_table": "trading.bt_mirror_ohlcv_daily",
      "refresh_group": "market",
      "refresh_strategy": "cursor_upsert",
      "cursor_column": "updated_at",
      "fallback_cursor_column": "trade_date",
      "source_sql": "SELECT o.ticker_id, t.symbol, o.trade_date, o.open, o.high, o.low, o.close, o.volume, o.updated_at FROM trading.ohlcv_daily o JOIN trading.ticker t ON t.id = o.ticker_id WHERE o.open IS NOT NULL AND o.high IS NOT NULL AND o.low IS NOT NULL AND o.close IS NOT NULL",
      "insert_sql": "INSERT INTO trading.bt_mirror_ohlcv_daily(ticker_id, symbol, trade_date, open, high, low, close, volume) VALUES (:ticker_id, :symbol, :trade_date, :open, :high, :low, :close, :volume) ON CONFLICT (ticker_id, trade_date) DO UPDATE SET symbol = EXCLUDED.symbol, open = EXCLUDED.open, high = EXCLUDED.high, low = EXCLUDED.low, close = EXCLUDED.close, volume = EXCLUDED.volume"
    },
    {
      "name": "macro_data_series",
      "source_table": "trading.macro_data_series",
      "target_table": "trading.macro_data_series",
      "refresh_group": "reference",
      "refresh_strategy": "resync",
      "cursor_column": "updated_at",
      "fallback_cursor_column": "id",
      "source_sql": "SELECT id, title AS name, frequency, units AS unit, is_active, created_at, updated_at FROM trading.macro_data_series",
      "insert_sql": "INSERT INTO trading.macro_data_series(id, name, frequency, unit, is_active, created_at, updated_at) VALUES (:id, :name, :frequency, :unit, :is_active, :created_at, :updated_at) ON CONFLICT (id) DO UPDATE SET name = EXCLUDED.name, frequency = EXCLUDED.frequency, unit = EXCLUDED.unit, is_active = EXCLUDED.is_active, updated_at = EXCLUDED.updated_at",
      "truncate_sql": "TRUNCATE TABLE trading.macro_data_series"
    },
    {
      "name": "macro_data_series_value",
      "source_table": "trading.macro_data_series_value",
      "target_table": "trading.macro_data_series_value",
      "refresh_group": "market",
      "refresh_strategy": "cursor_upsert",
      "cursor_column": "obs_date",
      "fallback_cursor_column": "obs_date",
      "source_sql": "SELECT id, series_id, obs_date, value, ingested_at AS created_at, ingested_at AS updated_at FROM trading.macro_data_series_value",
      "insert_sql": "INSERT INTO trading.macro_data_series_value(id, series_id, obs_date, value, created_at, updated_at) VALUES (:id, :series_id, :obs_date, :value, :created_at, :updated_at) ON CONFLICT (id) DO UPDATE SET series_id = EXCLUDED.series_id, obs_date = EXCLUDED.obs_date, value = EXCLUDED.value, updated_at = EXCLUDED.updated_at"
    }
  ],
  "exclude_prefixes": [
    "trading.order_",
    "trading.broker_",
    "trading.fill",
    "trading.execution",
    "trading.position",
    "trading.portfolio"
  ]
}
